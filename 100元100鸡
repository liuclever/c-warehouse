#include<stdio.h>
main()
{
	int g, m, x;
	printf("100可以买:\n");
	for(g=0;g<=20;g++)
		for(m=0;m<=33;m++)
			for (x = 0; x <= 100; x++)
			{
				if ((g * 5 + m * 3 + x * 1 / 3) == 100)
					printf("%d只公鸡%d只母鸡%d只小鸡\n", g, m, x);
			}
}
